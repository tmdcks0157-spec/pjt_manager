# 작업일지 2026-06-11

## 작업 내용

### 1. 다른 기기 개발 환경 세팅
- `.env.local` 파일 수동 생성 (git에 포함되지 않아 새 기기마다 직접 생성 필요)
- `npm install` 로 패키지 설치
- `npm run dev` 로 개발 서버 실행 및 로그인 확인

### 2. 프로젝트 수정 기능 추가
- 파일: `src/app/(main)/dashboard/page.tsx`
- 프로젝트 카드 호버 시 수정(연필) / 삭제(휴지통) 버튼 표시
- 수정 버튼 클릭 시 모달 팝업 — 이름 / 설명 / 색상 변경 가능
- Supabase `projects` 테이블 update 연동

### 3. 대시보드 통계 연동
- 파일: `src/app/(main)/dashboard/page.tsx`
- 상단 요약 카드 4개 추가
  - 전체 태스크 수
  - 완료 태스크 수
  - 오늘 마감 태스크 수
  - 기한 초과 태스크 수
- 프로젝트 카드에 진행률 바, 완료 수, 기한 초과 수 표시
- Supabase `tasks`, `columns` 테이블 조회하여 클라이언트에서 집계

### 4. 배포
- GitHub `main` 브랜치 push
- Vercel 자동 배포 완료 확인

## 커밋 내역
| 커밋 | 내용 |
|------|------|
| `0260ded` | feat: 대시보드 통계 및 프로젝트 수정 기능 추가 |
| `4eac549` | fix: 대시보드 Supabase 부분 select 타입 오류 수정 |
| `4b4136e` | feat: 태스크 카드 4가지 기능 추가 (이동/복사/펼치기/칸반 요약) |

### 5. 태스크 카드 기능 확장 (4가지)
- 파일: `src/app/(main)/projects/[id]/page.tsx`
- **다른 프로젝트로 이동**: 카드 호버 시 보라색 폴더 아이콘 → `MoveToProjectModal` → 대상 프로젝트 첫 컬럼으로 `project_id` + `status` 업데이트
- **카드 복사**: 초록색 복사 아이콘 → "(복사)" 접두사 + 동일 필드로 새 태스크 생성, `checklist_items`도 미완료 상태로 복사
- **설명/체크리스트 펼치기**: 설명 또는 체크리스트가 있는 카드에만 ChevronDown/Up 버튼 표시, 카드 내 인라인 미리보기 (설명 3줄, 체크리스트 4개)
- **칸반 요약 통계 바**: 헤더 아래 전체/완료/오늘마감/기한초과/진행률 표시 (태스크가 있을 때만 노출)

### 6. 배포
- GitHub `main` 브랜치 push → Vercel 자동 배포 완료

### 7. 프로젝트 보관/삭제 기능
- 파일: `src/app/(main)/dashboard/page.tsx`, `src/types/index.ts`
- 프로젝트 카드 호버 시 수정(연필) / 보관(박스) / 삭제(휴지통) 버튼 표시
- 보관: `projects.archived = true` 업데이트 → 활성 목록에서 제거
- 보관 해제: `archived = false` 로 복구
- 삭제: hard delete (더 강한 경고 문구)
- 대시보드 하단 "보관된 프로젝트" 접힘/펼치기 섹션 (개수 뱃지)
- Supabase: `projects` 테이블에 `archived boolean DEFAULT false` 컬럼 기존 존재 확인

### 8. 배포
- GitHub `main` 브랜치 push (`8047a6f`) → Vercel 자동 배포 완료

## 커밋 내역
- `.env.local`은 `.gitignore`에 포함되어 있어 새 기기마다 수동 생성 필요
- Supabase 부분 select 시 TypeScript 타입을 `Pick<T, ...>` 또는 `as` 캐스팅으로 명시해야 Vercel 빌드 통과
- 완료 태스크 판별: `columns` 테이블에서 `name === '완료'`인 컬럼 ID를 기준으로 집계
- 다른 프로젝트로 이동 시 대상 프로젝트의 첫 컬럼을 on-demand로 조회 (`order` 기준 `limit 1`)
- 카드 펼치기 상태는 React 로컬 state (`useState`) — 새로고침 시 초기화 (의도적)
