# 작업일지 2026-06-21

## 작업 개요

오늘 페이지 개선 + 설정 비밀번호 변경 + 문서 정리

---

## 1. Today — 미완료 태스크 기본 펼침

- 파일: `src/app/(main)/today/page.tsx`
- `useState(false)` → `useState(true)` 변경
- 이전에는 마감일 없는 태스크 섹션이 접힌 상태로 시작되어 불편했음

---

## 2. 설정 — 비밀번호 변경 기능

- 파일: `src/app/(main)/settings/page.tsx`
- 현재 비밀번호 `signInWithPassword` 로 검증 후 `supabase.auth.updateUser({ password })` 호출
- Eye/EyeOff 토글, Enter 키 지원, 성공/실패 피드백 UI
- 사용자 직접 테스트 완료

---

## 3. Today — 이번 주 마감 예정 섹션 추가

- 파일: `src/app/(main)/today/page.tsx`
- 내일 ~ 이번 주 일요일 마감 태스크를 날짜순으로 우측 컬럼 상단에 표시
- "내일" 또는 "수 6/24" 형태 D-day 레이블
- `isEmpty` 조건에 `weekDueTasks.length === 0` 포함

---

## 4. 문서 정리

- `README.md` 재작성 (boilerplate → 실제 프로젝트 문서)
- 작업일지 `docs/worklog/` 폴더로 통합
- 외부 PDCA 문서 프로젝트 내부로 이동
- `docs/아이디어_기능및프로그램.md` → `docs/ideas.md` 최신화

---

## 발견된 기존 구현 (새로 만들 필요 없었음)

- **CRM 연락처 tel/mailto**: `ContactCard`에 이미 `href="tel:..."` / `href="mailto:..."` 링크 존재
- **전체 현황 태스크 클릭**: 이미 우측 DetailPanel 편집 패널 동작 중

---

## 커밋 내역

| 커밋 | 내용 |
|------|------|
| `d6461d3` | feat: 오늘 페이지 이번 주 마감 예정 섹션 추가 |
| `0965dbb` | chore: playwright 패키지 추가 및 스크린샷 스크립트 |
| `2a654ec` | docs: README 프로젝트 실제 상태로 재작성 |

---

## 기술 메모

- CRM `ContactCard` 클릭 vs 링크 이벤트: `onClick={e => e.stopPropagation()}` 로 버블링 방지
- `weekEnd` 계산: `getDay() === 0` (일요일) 예외처리 필요 (daysUntilSunday = 0)
- Playwright 스크린샷 스크립트: `scripts/screenshot.mjs` (로컬 서버 실행 중일 때만 사용)
